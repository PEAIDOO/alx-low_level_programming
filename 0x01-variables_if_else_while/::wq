#include <stdio.h>

/**
 * main - main is here
 *
 * Return: 0 lives here
 */
int main(void)
{
	int i;
	char z;

	for (i = 0; i <= 9; i++)
		putchar(i + '0');
	for (z = 'a'; a <= 'f'; a++)
		putchar(a);
	putchar('\n')
		return (0);
}
